__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

.DS_Store
.idea/
.vscode/

# v0.2 verification drivers (kept locally, not part of release)
_run_pipeline.py
_backfill.sh

# Per-run pipeline cache (regenerated on each run)
reports/.cache/

# Local secrets — copy from .env.example to fill in
.env

# FAISS / sentence-transformers index built lazily by --match-mode embedding
metadb/.embeddings/

# fcntl advisory lock for RollingDB
metadb/.lock
